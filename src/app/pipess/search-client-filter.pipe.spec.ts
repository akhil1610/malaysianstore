import { SearchClientFilterPipe } from './search-client-filter.pipe';

describe('SearchClientFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchClientFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
